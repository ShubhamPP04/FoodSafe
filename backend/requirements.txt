# FoodSafe backend — production runtime dependencies only.
#
# The app runs on FastAPI + MongoDB Atlas (via motor) + Gemini/Synthetic
# HTTP calls (via httpx). No torch/ultralytics/spacy/chromadb/celery/prophet
# are used at runtime — those live in requirements-full.txt for anyone who
# wants to run the old local ML stack, dev notebooks, or scraper scripts.
#
# This lean file is what makes the API deployable as a Vercel serverless
# function (well under the platform's size limits) — it's also just a
# faster, lighter install for Docker/Render.

fastapi==0.109.2
starlette==0.36.3
uvicorn==0.27.1
pydantic==2.9.0
pydantic-settings==2.9.0

# MongoDB Atlas driver
motor==3.6.0

# Outbound HTTP (Gemini + Synthetic fallback + Open Food Facts + scraping)
httpx==0.28.1
requests==2.32.2

# Auth (JWT) — password hashing uses stdlib hashlib.pbkdf2_hmac, no bcrypt needed
python-jose==3.3.0

# FastAPI file uploads (UploadFile / Form)
python-multipart==0.0.9

# Local .env loading
python-dotenv==1.0.1

# FSSAI scraper (BeautifulSoup + RSS/HTML parsing)
beautifulsoup4==4.12.3
lxml==6.0.2

# Personalized risk scorer (small RandomForest model in ml/risk_scorer_rf.joblib)
joblib==1.3.2
scikit-learn==1.6.1
numpy==1.26.4

# WhatsApp bot (Twilio) + weekly digest messages
twilio==8.13.0

# Web push notifications
pywebpush==2.0.0

# Scheduled jobs run in-process (no broker needed) — kept only so
# tasks/*.py's `@celery_app.task` decorators still import cleanly and
# `.run()` can execute them synchronously from routers/cron.py.
celery==5.3.6
